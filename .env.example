# Copy this file to .env and set your Claude API key there.
CLAUDE_API_KEY=your_claude_api_key_here
# Optional: override the Claude model if needed.
CLAUDE_MODEL=claude-3.5
# Optional: override the Claude endpoint if your account uses a different endpoint.
CLAUDE_API_URL=https://api.anthropic.com/v1/complete
